{
  "name": "openai-image-remote-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server (Streamable HTTP) that generates/edits images with your OpenAI key, hosts them on Vercel Blob, and returns public URLs. For use as a claude.ai custom connector.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "@vercel/blob": "^0.27.0",
    "mcp-handler": "^1.0.1",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
